[workspace]
members = [
    "abi",
    "api",
    "cache",
    "msg_server",
    "cmd",
    "consumer",
    "db",
    "oss",
    "utils",
    "msg_gateway",
]
resolver = "2"
default-members = ["cmd"]

[profile.dev]
incremental = true
